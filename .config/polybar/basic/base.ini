[basic/base]
; GLOBAL
label-background = ${color.transparent}
label-foreground = ${color.brightwhite}
ramp-foreground = ${color.brightwhite}
ramp-background = ${color.transparent}
tray-background = ${color.transparent}
border-color = ${color.transparent}

; NETWORK
label-connected-background = ${color.transparent}
label-connected-foreground = ${color.brightwhite}
label-disconnected-background = ${color.transparent}
label-disconnected-foreground = ${color.brightwhite}

; TEMPERATURE
label-warn-background = ${color.transparent}
label-warn-foreground = ${color.brightred}

; BATTERY
label-full-foreground = ${color.brightgreen}
label-full-background = ${color.transparent}
label-charging-foreground = ${color.brightwhite}
label-charging-background = ${color.transparent}
label-discharging-foreground = ${color.brightwhite}
label-discharging-background = ${color.transparent}
ramp-capacity-0-foreground = ${color.red}
ramp-capacity-0-background = ${color.transparent}
ramp-capacity-1-foreground = ${color.yellow}
ramp-capacity-1-background = ${color.transparent}
ramp-capacity-2-foreground = ${color.brightyellow}
ramp-capacity-2-background = ${color.transparent}
animation-charging-foreground = ${color.brightgreen}
animation-charging-background = ${color.transparent}
animation-discharging-foreground = ${color.brightwhite}
animation-discharging-background = ${color.transparent}

; AUDIO
label-muted-background = ${color.transparent}
label-muted-foreground = ${color.brightwhite}
label-volume-background = ${color.transparent}
label-volume-foreground = ${color.brightwhite}

; WORKSPACE
label-mode-background = ${color.brightwhite}
label-mode-foreground = ${color.transparent}
label-active-foreground = ${color.brightwhite}
label-active-background = ${color.transparent}
label-focused-foreground = ${color.brightwhite}
label-focused-background = ${color.brightblue}
label-unfocused-foreground = ${color.transparent}
label-unfocused-background = ${color.brightwhite}
label-visible-foreground = ${color.transparent}
label-visible-background = ${color.brightwhite}
label-urgent-foreground = ${color.brightred}
label-urgent-background = ${color.brightwhite}
label-dimmed-foreground = ${color.transparent}
label-dimmed-background = ${color.brightwhite}
label-dimmed-underline  = ${color.brightblue}
label-monitor-foreground = ${color.transparent}
label-monitor-background = ${color.brightwhite}
label-occupied-foreground = ${color.transparent}
label-occupied-background = ${color.brightwhite}
label-empty-foreground = ${color.white}
label-empty-background = ${color.transparent}

; TEXT
content-background = ${color.transparent}
content-foreground = ${color.brightwhite}

; WINDOW
; label-empty-background = ${color.brightwhite}
; label-empty-foreground = ${color.black}
